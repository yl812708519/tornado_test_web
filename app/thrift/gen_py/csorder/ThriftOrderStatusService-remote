#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import ThriftOrderStatusService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string gets_by_order_type(string order_type)'
  print '  bool update_order_status(i64 order_id, string status, i64 stuff_id)'
  print '  string get_order_status()'
  print '  string get_biz_status_by_type(string order_type, bool is_paid_include)'
  print '  string get_dict_by_order_types( order_type)'
  print '  void reset_confirm(i64 stuff_id, i64 order_id, string order_type)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = ThriftOrderStatusService.Client(protocol)
transport.open()

if cmd == 'gets_by_order_type':
  if len(args) != 1:
    print 'gets_by_order_type requires 1 args'
    sys.exit(1)
  pp.pprint(client.gets_by_order_type(args[0],))

elif cmd == 'update_order_status':
  if len(args) != 3:
    print 'update_order_status requires 3 args'
    sys.exit(1)
  pp.pprint(client.update_order_status(eval(args[0]),args[1],eval(args[2]),))

elif cmd == 'get_order_status':
  if len(args) != 0:
    print 'get_order_status requires 0 args'
    sys.exit(1)
  pp.pprint(client.get_order_status())

elif cmd == 'get_biz_status_by_type':
  if len(args) != 2:
    print 'get_biz_status_by_type requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_biz_status_by_type(args[0],eval(args[1]),))

elif cmd == 'get_dict_by_order_types':
  if len(args) != 1:
    print 'get_dict_by_order_types requires 1 args'
    sys.exit(1)
  pp.pprint(client.get_dict_by_order_types(eval(args[0]),))

elif cmd == 'reset_confirm':
  if len(args) != 3:
    print 'reset_confirm requires 3 args'
    sys.exit(1)
  pp.pprint(client.reset_confirm(eval(args[0]),eval(args[1]),args[2],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
